# TODO

## High Priority

- Replace all placeholder identity content:
  - name in navbar/layout metadata
  - email/GitHub/LinkedIn in contact page
- Replace sample project entries in `src/data/projects.ts` with real projects
- Add `public/resume.pdf`

## Medium Priority

- Add local screenshots in `public/images/projects/` and use local paths
- Add per-project detail pages (`/projects/[slug]`)
- Add project sort options (year/status) in projects view
- Add favicon and social preview image

## Low Priority

- Add optional contact form integration
- Add CI workflow for lint/build on push
- Add analytics (privacy-friendly)
